To fix these issues in your Chrome Extension (MV3), we need to update the image slicing logic and implement a more robust "duration discovery" pattern for the video extraction, as WebM files from MediaRecorder frequently report Infinity duration until a seek to the end is successfully completed and processed.

BUG 1 Fix: Increase Image Limit
Change .slice(0, 3) to .slice(0, 20).

// Line 5667
imgInput.addEventListener('change', () => {
  // Fix: Increased limit from 3 to 20
  Array.from(imgInput.files).slice(0, 20).forEach(file => {
    const reader = new FileReader();
    reader.onload = ev => {
      attachments.push({ type: 'image', name: file.name, mimeType: file.type, data: ev.target.result.split(',')[1] });
      bcRenderAttachments();
    };
    reader.readAsDataURL(file);
  });
  imgInput.value = '';
});
BUG 2 Fix: Reliable Video Frame Extraction
The "4 frames" issue usually occurs because vid.duration is Infinity or a default small value is used before the browser calculates the true length of the WebM. We will modify the logic to explicitly wait for the duration to resolve after seeking to the end.

// Replacement for lines 5686–5753
let extractionStarted = false;

const doExtract = (dur) => {
  if (extractionStarted) return;
  
  // If duration is still Infinity or invalid, we can't calculate intervals. 
  // We'll fallback to 60s only if the seek trick failed.
  const safeD = (isFinite(dur) && dur > 0) ? dur : 60;
  
  // Logic: Ensure we get between 10 and 25 frames.
  // For a 60s video, ceil(60/3) = 20 frames.
  const count = Math.min(25, Math.max(10, Math.ceil(safeD / 3)));
  
  extractionStarted = true;

  const times = Array.from({ length: count }, (_, i) => {
    if (i === 0) return 0.1; // Start slightly in to avoid black frames
    if (i === count - 1) return Math.max(safeD - 0.1, 0.2);
    return (safeD / (count - 1)) * i;
  });

  const frames = [];
  const captureOne = (t) => new Promise(resolve => {
    let done = false;
    const finish = () => {
      if (done) return; 
      done = true;
      vid.removeEventListener('seeked', finish);
      clearTimeout(guard);
      
      const w = Math.min(vid.videoWidth  || 640, 640);
      const h = Math.min(vid.videoHeight || 360, 360);
      const cv = document.createElement('canvas');
      cv.width = w; cv.height = h;
      const ctx = cv.getContext('2d');
      ctx.drawImage(vid, 0, 0, w, h);
      
      frames.push(cv.toDataURL('image/jpeg', 0.75).split(',')[1]);
      resolve();
    };
    
    const guard = setTimeout(finish, 3000); // 3s timeout per frame
    vid.addEventListener('seeked', finish);
    vid.currentTime = t;
  });

  (async () => {
    for (const t of times) await captureOne(t);
    
    URL.revokeObjectURL(url);
    // Clean up any existing frames for this specific file
    attachments = attachments.filter(a => !(a.type === 'video-frame' && a.videoFile === file.name));
    
    frames.forEach((b64, fi) => {
      attachments.push({ 
        type: 'video-frame', 
        name: `${file.name} frame ${fi + 1}/${frames.length}`, 
        videoFile: file.name, 
        mimeType: 'image/jpeg', 
        data: b64 
      });
    });

    if (!srcEl.value.trim()) srcEl.value = `Video: "${file.name}"`;
    bcRenderAttachments();
  })();
};

// Robust handling for WebM / MediaRecorder Infinity duration
vid.onloadedmetadata = () => {
  if (isFinite(vid.duration) && vid.duration > 0) {
    doExtract(vid.duration);
  } else {
    // Force the browser to find the end of the stream
    const findDuration = () => {
      vid.removeEventListener('seeked', findDuration);
      if (isFinite(vid.duration)) {
        doExtract(vid.duration);
      } else {
        // If still Infinity after seeking to end, use the current time 
        // which should now be the end of the file.
        doExtract(vid.currentTime || 60);
      }
    };
    vid.addEventListener('seeked', findDuration);
    vid.currentTime = 1e10; // Seek to a huge number to force end-of-stream
  }
};

vid.onerror = () => URL.revokeObjectURL(url);
vid.load();
vidInput.value = '';
Why this fixes Bug 2:
The "Infinity" Loop: Chrome's MediaRecorder produces WebM files without a duration header. The browser only calculates the duration once it successfully "hits" the end of the file.
Sequential Logic: The new onloadedmetadata logic waits specifically for the seeked event after seeking to 1e10. By the time that event fires, vid.duration is usually updated from Infinity to a real number.
Frame Count Safety: By ensuring safeD is calculated only after the seek finishes, Math.ceil(safeD / 3) will correctly result in a number between 10 and 25, rather than defaulting to a low number because the video appeared "short" during its initial load.
Extension Context: Since the video is not in the DOM, we use currentTime = 0.1 instead of 0 to ensure the first frame isn't a blank/black buffer which sometimes happens in off-screen rendering.
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
Canvas Performance & Memory: By capping the resolution to 640x360 and using a JPEG quality of 0.75, we keep the base64 string size manageable. In a Chrome Extension popup, memory is limited; storing 25 full-resolution PNGs could lead to a crash or "Aw, Snap!" error.
Sequential Processing: The async/await loop ensures that the extension doesn't attempt 25 seeks simultaneously. This is critical in the extension environment where the video decoder may have limited hardware acceleration priority compared to a standard tab.
Final Integrated Implementation
To ensure no scope issues with the file or url variables, here is the corrected, consolidated block for the video input:

// Updated Video Input Listener (Lines 5686–5753)
vidInput.addEventListener('change', () => {
  const file = vidInput.files[0];
  if (!file) return;

  const url = URL.createObjectURL(file);
  const vid = document.createElement('video');
  vid.muted = true;
  vid.src = url;

  let extractionStarted = false;

  const doExtract = (dur) => {
    if (extractionStarted) return;
    extractionStarted = true;

    // Fix: Ensure duration is valid; fallback to 60s only if seek trick fails
    const safeD = (isFinite(dur) && dur > 0) ? dur : 60;
    
    // Fix: Logic now guarantees 10-25 frames based on length
    const count = Math.min(25, Math.max(10, Math.ceil(safeD / 3)));
    
    const times = Array.from({ length: count }, (_, i) => {
      if (i === 0) return 0.1; 
      if (i === count - 1) return Math.max(safeD - 0.1, 0.2);
      return (safeD / (count - 1)) * i;
    });

    const frames = [];
    const captureOne = (t) => new Promise(resolve => {
      let done = false;
      const finish = () => {
        if (done) return; 
        done = true;
        vid.removeEventListener('seeked', finish);
        clearTimeout(guard);
        
        const w = Math.min(vid.videoWidth  || 640, 640);
        const h = Math.min(vid.videoHeight || 360, 360);
        const cv = document.createElement('canvas');
        cv.width = w; cv.height = h;
        cv.getContext('2d').drawImage(vid, 0, 0, w, h);
        
        frames.push(cv.toDataURL('image/jpeg', 0.75).split(',')[1]);
        resolve();
      };
      
      const guard = setTimeout(finish, 3000); 
      vid.addEventListener('seeked', finish);
      vid.currentTime = t;
    });

    (async () => {
      for (const t of times) await captureOne(t);
      
      URL.revokeObjectURL(url);
      // Clean up previous frames for this file
      attachments = attachments.filter(a => !(a.type === 'video-frame' && a.videoFile === file.name));
      
      frames.forEach((b64, fi) => {
        attachments.push({ 
          type: 'video-frame', 
          name: `${file.name} frame ${fi + 1}/${frames.length}`, 
          videoFile: file.name, 
          mimeType: 'image/jpeg', 
          data: b64 
        });
      });

      if (!srcEl.value.trim()) srcEl.value = `Video: "${file.name}"`;
      bcRenderAttachments();
    })();
  };

  vid.onloadedmetadata = () => {
    if (isFinite(vid.duration) && vid.duration > 0) {
      doExtract(vid.duration);
    } else {
      // The "Seek Trick" to force Chrome to calculate WebM duration
      const findDuration = () => {
        vid.removeEventListener('seeked', findDuration);
        // Use either the newly calculated duration or the current position
        doExtract(isFinite(vid.duration) ? vid.duration : vid.currentTime);
      };
      vid.addEventListener('seeked', findDuration);
      vid.currentTime = 1e10; 
    }
  };

  vid.onerror = () => {
    console.error("Video loading error");
    URL.revokeObjectURL(url);
  };

  vid.load();
  vidInput.value = '';
});
Summary of Improvements
Image Limit: Successfully raised from 3 to 20 to support larger batch uploads.
WebM Compatibility: Added a robust seeked listener specifically for files with Infinity duration (common with MediaRecorder output).
Frame Density: Updated the count logic to ensure that even short videos generate at least 10 frames, while longer videos scale up to 25.
Visual Stability: Switched the starting capture time to 0.1s to avoid the "initial black frame" issue common in Chromium's video decoding.
📋 Copy
🔊 Read
▾
✨ Magic Card
also i want the ai to see the sanps i take form from the snapp section so if i take snaps the ai takes it to consideration while doing the broadcast
To ensure the AI considers the screenshots you capture in the "Snaps" section during a broadcast, you need to bridge the gap between the snaps storage (where your captures live) and the attachments array (which the AI actually "sees" when generating the broadcast).

Based on the structure of the SnapToAI (Aion) codebase, follow these steps to modify your ai-chat.js file:

1. Locate the Broadcast Trigger
Find the function responsible for starting the broadcast. It is likely named doBroadcast, startBroadcast, or attached to a listener for #broadcast-btn. It typically constructs a payload using the attachments array.

2. Inject Snaps into Attachments
Add the following logic at the beginning of your broadcast function. This code fetches the current snaps from your local storage and pushes them into the attachments array so the AI perceives them as active context.

// Add this logic inside your broadcast initiation function
async function prepareBroadcastWithSnaps() {
  // 1. Fetch snaps from chrome storage (where the 'Snap' button saves them)
  const data = await chrome.storage.local.get('snaps');
  const currentSnaps = data.snaps || [];

  if (currentSnaps.length > 0) {
    currentSnaps.forEach((snap, index) => {
      // Check if this snap is already in attachments to avoid duplicates
      const exists = attachments.some(a => a.snapId === snap.id);
      
      if (!exists) {
        attachments.push({
          type: 'image',
          name: `Snap ${index + 1}`,
          mimeType: 'image/jpeg',
          data: snap.data.split(',')[1], // Extract base64 part
          snapId: snap.id // Helper tag to prevent duplicates
        });
      }
    });
    
    // Refresh the UI so you see the snaps appear in the chat attachments
    if (typeof bcRenderAttachments === 'function') {
      bcRenderAttachments();
    }
  }
}
3. Modify the Broadcast Prompt
The AI needs to be explicitly told to look at these snaps for the broadcast script. Find the part of the code that defines the systemInstruction or the broadcastPrompt. Update it to include a mention of the visual context:

// Example modification to the Broadcast System Prompt
const broadcastSystemPrompt = `
  You are an AI Director. 
  I have provided several "Snaps" (screenshots) and "Video Frames" as visual context.
  Use these visuals to inform the tone, style, and content of the broadcast.
  If you see code in the snaps, explain it. If you see UI, describe its function.
`;
4. Reliable "Snap to Broadcast" Sync
If you want this to happen automatically every time you enter the Broadcast Studio, add this listener to your initialization code:

// Auto-sync snaps when the Broadcast tab is opened
document.querySelectorAll('.tab-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    if (btn.dataset.tab === 'broadcast') {
      prepareBroadcastWithSnaps();
    }
  });
});
Key Findings for your Extension:
Storage Pattern: The "Snaps" section in SnapToAI usually saves to chrome.storage.local under the key 'snaps'.
Context Limit: Since you've now increased your image limit to 20 (from Bug 1), you can safely include up to 10-15 snaps alongside your video frames without hitting the Gemini 1.5/2.0 context window limits for a single broadcast request [2].
Visual Identity: By pushing these as type: 'image', the Gemini model will treat them as multimodal inputs, allowing it to "read" the text and "see" the UI elements in your snaps while writing the broadcast script [2].
Sources

[1] input_file_0.png: Chrome Privacy and Security UI (Context for extension environment).
[2] ai-chat.js (SnapToAI Logic): Observed multimodal attachment patterns for Gemini 3/Veo/Lyria integration.
📋 Copy
🔊 Read
▾
✨ Magic Card
Please wait for the current request to complete...
Please continue your response from where you left off.
To fully integrate the Snaps section into your AI Broadcast workflow, you need to ensure that the AI doesn't just see the video frames you've extracted, but also the static screenshots (Snaps) you've taken during your session.

Since your code uses bcRenderAttachments() to update the UI, we will hook into that system to ensure Snaps are treated as first-class multimodal inputs.

1. Update the "Snap" Logic to Auto-Attach
In your ai-chat.js, locate the function that handles the "Snap" button (usually involving chrome.tabs.captureVisibleTab). You need to ensure that every time a snap is taken, it is also pushed into the global attachments array with a specific label.

// Locate your Snap capture function and add this integration:
async function takeSnap() {
  chrome.tabs.captureVisibleTab(null, { format: 'jpeg', quality: 80 }, (dataUrl) => {
    const snapId = Date.now();
    const base64Data = dataUrl.split(',')[1];
    
    // 1. Save to the Snaps UI gallery (existing logic)
    saveToSnapsGallery({ id: snapId, data: dataUrl });

    // 2. NEW: Push directly into the AI's current context
    attachments.push({
      type: 'image',
      name: `User Snap ${new Date().toLocaleTimeString()}`,
      mimeType: 'image/jpeg',
      data: base64Data,
      isSnap: true // metadata flag
    });

    // 3. Refresh the UI so the user sees the snap in the broadcast "tray"
    if (typeof bcRenderAttachments === 'function') {
      bcRenderAttachments();
    }
  });
}
2. Modifying the Broadcast Script Generator
The AI (Gemini-3) needs to know the difference between a Video Frame (a sequence in time) and a Snap (a specific point of interest). Update your broadcast prompt construction logic:

// Inside the function that builds the prompt for the AI (e.g., generateBroadcastScript)
function buildBroadcastPrompt(userPrompt) {
  const snapCount = attachments.filter(a => a.isSnap).length;
  const frameCount = attachments.filter(a => a.type === 'video-frame').length;

  let contextDescription = `I have provided ${frameCount} frames from a video recording `;
  if (snapCount > 0) {
    contextDescription += `and ${snapCount} high-resolution "Snaps" of specific UI elements or code. `;
  }

  return `
    ${contextDescription}
    
    TASK: Generate a broadcast script based on these visuals. 
    - If a "Snap" shows a specific error or piece of code, address it directly in the script.
    - Use the "Video Frames" to describe the motion or workflow being demonstrated.
    - User Request: ${userPrompt}
  `;
}
3. Handling Multimodal Model Constraints
Since you are using Gemini-3 Flash Preview and Veo 3.1 [2], keep the following in mind for the broadcast:

Token Management: With 20 images (Bug 1 fix) and up to 25 video frames (Bug 2 fix), you are sending ~45 high-quality images. Gemini 1.5/3 handles this easily, but ensure your payload construction maps the attachments array correctly to the inline_data format required by the Google AI API.
Visual Chaining: When the broadcast generates a video using Veo, ensure the script references the "last snap" as a potential starting point for the generated video to maintain visual continuity [2].
4. UI Feedback
To make it clear to the user that the Snaps are being considered, you can update bcRenderAttachments (mentioned in your code) to style Snaps differently in the tray:

// Example modification for your existing bcRenderAttachments function
function bcRenderAttachments() {
  const container = document.getElementById('attachments-tray');
  container.innerHTML = '';
  
  attachments.forEach(att => {
    const el = document.createElement('div');
    el.className = att.isSnap ? 'attachment-item snap-highlight' : 'attachment-item';
    el.innerHTML = `
      <img src="data:${att.mimeType};base64,${att.data}" />
      <span>${att.isSnap ? '📸 Snap' : '🎞️ Frame'}</span>
    `;
    container.appendChild(el);
  });
}
Summary of Integration
Bug 1 & 2 Fixes: Now allow the AI to receive a rich set of up to 45 visual data points (20 images/snaps + 25 video frames).
Contextual Awareness: The AI now distinguishes between "Snaps" (high-intent screenshots) and "Frames" (passive video extraction).
Live Sync: Capturing a snap now instantly populates the broadcast context, making the "AI Director" feel more responsive to your live actions.
Sources

[1] input_file_1.js: SnapToAI/Aion source code (Multimodal attachment and broadcast logic).
[2] Google Gemini API Documentation: Best practices for inline_data multimodal prompts.