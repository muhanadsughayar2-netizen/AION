able Google Cloud billing → Google provides $300 in credits
Add Billing
✕
◀
Screenshot
Screenshot
Tap image → send to chat
Here is the complete map of every piece of code for both systems: 🔑 API Setup — Google AI Studio Key flow-premium/popup.js Lines What it does 3739–3754 showGeminiModal() / hideGeminiModal() — opens/closes the key entry popup 3756–3776 loadGeminiKey() — reads saved key from chrome.storage.sync on load 3779–3814 _popupProbeOneVeo() — pings Veo API to detect if key is free-tier or prepaid 4097 Auto-shows modal when no key is set 4111 "Set up API key" banner button → triggers showGeminiModal() flow-premium/ai-chat.js Lines What it does 430–560 showProxyKeyPrompt() — same modal call from inside the chat panel; runs the key tier probe, shows green (prepaid) or orange (free) result card with " {apiKey} 🏗️ Build Section (App Builder) State variables — ai-chat.js lines 897–902 + 8806–8828 buildModeEnabled — true/false toggle buildStage — null / 'L1' / 'L2' / 'L3' / 'UPDATE' _lastBuiltCode — the full HTML of the current built site _lastBuiltCodeForPatch — same but with SNAP_VID_N placeholders intact (no base64) _committedMediaMap — all media already embedded in the current build _pendingBuildImages/Videos/Audio — base64 waiting to swap in after AI responds The prompts — ai-chat.js Lines Constant Purpose 5353–5724 BUILD_SYSTEM_PROMPT Full single-file build from scratch — picks aesthetic profile (A–E), enforces Tailwind + Lucide + Pexels, 32k tokens 5728–5749 L1_SCAFFOLD_PROMPT Stage 1 — outputs only <body> HTML 5751–5768 L2_DESIGN_PROMPT Stage 2 — outputs only CSS 5770–5814 L3_ACTIVATE_PROMPT Stage 3 — outputs only JavaScript 5816–5833 BUILD_PATCH_PROMPT Surgical patch — change only what user asked, preserve everything else, IIFE wrapper enforced 5835–5843 L_UPDATE_PROMPT Section-level update — replaces one data-section-id block only Build toggle button — ai-chat.js lines 9138–9162 #buildToggleBtn click → toggles buildModeEnabled → restores _lastBuiltCode from storage if re-opening Stage buttons — ai-chat.js lines 9165+ L1 / L2 / L3 buttons → _setStage('L1'/'L2'/'L3') → sets buildStage → updates chat placeholder How it fires in handleSend() — ai-chat.js lines 7363–7404 buildStage = 'L1' → L1_SCAFFOLD_PROMPT buildStage = 'L2' → L2_DESIGN_PROMPT (+ injects _buildBodyHtml) buildStage = 'L3' → L3_ACTIVATE_PROMPT (+ injects assembled HTML) buildStage = 'UPDATE'→ L_UPDATE_PROMPT (+ injects _lastBuiltCode) no stage, existing site → BUILD_PATCH_PROMPT (surgical edit) no stage, no site → BUILD_SYSTEM_PROMPT (fresh build) Storage — persists across popup close/reopen chrome.storage.local: snaptoai_built_code → _lastBuiltCode snaptoai_build_body → _buildBodyHtml snaptoai_build_style → _buildStyleCss snaptoai_build_script → _buildScriptJs Media staging (image/video → embed in site) ai-chat.js lines 8844–8862: setStagedMedia(type, mimeType, base64, label) — stores + shows indicator clearStagedMedia() — consumed after one build "📌 Use in Build" buttons on images/videos — lines 7046, 4801 That's every single piece. Tell me what you want to change or fix and I'll go straight to the exact lines. i a very intrested in making my build work on the app however we build but its always hard to have it work properly is tehre a way to make it work please look at the code and tell me why it is not working like antigravity pelase ifwecan make it like anti gravitoy funtins since i am representing google ai strudio why is it not working fine please tell me what to tell replit to do to make the system build like agentic important pelase
Summary
Your Chrome Extension (SnapToAI / Aion) is currently struggling to build fully functional, reliable applications because it uses a single-shot static code generation architecture. In contrast, Google's Antigravity Agent represents a paradigm shift to managed agentic workflows.

To make your extension build apps like Antigravity, you need to transition your backend (such as on Replit) from standard chat completion endpoints (streamGenerateContent) to the Gemini Interactions API utilizing the antigravity-preview-05-2026 model. This will allow the AI to autonomously plan, write, execute, test, and self-correct code inside a secure, Google-hosted Linux sandbox.

Key Findings: Why Your App Isn't Working Like Antigravity
Based on an analysis of your extension's codebase (input_file_1.js and input_file_2.js) and the Google AI Studio environment, here is why your current system struggles to match the reliability and power of Antigravity:

1. Single-Shot Generation vs. Autonomous Loop
Your App's Flow: In input_file_2.js (around line 7430), your app calls the standard streamGenerateContent endpoint [2]. It sends a system prompt (like BUILD_SYSTEM_PROMPT or L1_SCAFFOLD_PROMPT) [2], receives a block of code, and displays it inside an iframe [2]. If there is a syntax error, a broken library, or a runtime bug, the AI has no way of knowing [2].
Antigravity's Flow: Antigravity does not just output text. A single API call triggers an autonomous loop of planning, reasoning, tool execution, and file management. It writes the code, runs it, checks the output, observes errors, and self-corrects until the code runs perfectly.
2. No Runtime Environment or Sandbox
Your App's Flow: Your app is limited to front-end, single-file HTML/CSS/JS rendered locally in a browser iframe (sandbox.html) [2]. It cannot run backend servers, install npm packages, or execute Node.js, Python, or Go code [2].
Antigravity's Flow: Antigravity provisions a secure, isolated Linux sandbox hosted by Google. It can run terminal commands, execute scripts, spin up servers, and manage multi-file project structures.
3. No Full-Stack or Database Integration
Your App's Flow: Your app has no database [2]. Any data generated in your built sites is lost on reload [2].
Antigravity's Flow: Antigravity natively integrates with Firebase (Firestore and Authentication). With a single prompt, the agent can configure a real database, set up user sign-in, and persist data.
How to Make It Work: What to Tell Replit to Build
To upgrade your system to a truly agentic builder, you should host a backend server on Replit that acts as the "Agent Orchestrator" for your Chrome Extension. Copy and paste the following blueprint directly into Replit to guide your development:

1. Migrate to the Gemini Interactions API
Instruct Replit to rewrite the code-generation backend to use the Interactions API with the antigravity-preview-05-2026 model. This provisions a persistent Linux sandbox on Google's servers.

Node.js Implementation for Replit Backend:

import { GoogleGenAI } from "@google/genai";

const client = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });

// This creates or resumes a persistent agentic environment
const interaction = await client.interactions.create({
  agent: "antigravity-preview-05-2026",
  input: "Build a responsive web application based on this prompt. Run it, verify there are no console errors, and package it as a ZIP file.",
  environment: "remote", // Provisions a secure Google-hosted Linux sandbox
}, { timeout: 300000 });

console.log(interaction.output_text);
Direct REST API Call (Alternative):

curl -X POST "https://generativelanguage.googleapis.com/v1beta/interactions" \
  -H "Content-Type: application/json" \
  -H "x-goog-api-key: $GEMINI_API_KEY" \
  -H "Api-Revision: 2026-05-20" \
  -d '{
    "agent": "antigravity-preview-05-2026",
    "input": "Create a fully functional Kanban board app. Run tests to ensure all buttons work.",
    "environment": "remote"
  }'
2. Implement a "Self-Healing" Loop on Replit
If you choose to run the agentic loops locally on your Replit workspace rather than relying entirely on Google's remote sandbox, tell Replit to build a Self-Healing compiler:

Catch Iframe Errors: Inject a small script into your extension's preview iframe to catch all runtime console errors:
window.onerror = function(message, source, lineno, colno, error) {
  // Send this error back to your Replit server via WebSocket
  window.parent.postMessage({ type: 'CONSOLE_ERROR', error: message, line: lineno }, '*');
};
Auto-Correction Loop: When your Replit server receives a CONSOLE_ERROR, it should immediately fire a prompt to Gemini:
Prompt: `"The code you just generated threw this error: [ERROR] on line [LINE]. Analyze the existing code and output the corrected version."*
Repeat: Do this automatically behind the scenes until the iframe reports 0 console errors.
3. Build a Multi-File Workspace on Replit
Instead of forcing the AI to cram everything into a single file (which limits design quality and causes token cutoff issues) [2], have your Replit backend:

Manage a Vite / React / Next.js workspace.
Have the
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
agent use specific filesystem tools to read, write, and patch individual files. This avoids forcing the model to rewrite thousands of lines of code when only a small change is needed. 3. Compile and bundle the project dynamically. Once the files are updated, Replit can run a build command (like npm run build or vite build). If the compiler throws an error, feed that error directly back to the agent to self-correct. 4. Serve the preview. Instead of rendering a single massive HTML string in an iframe, Replit can host the built files on a temporary deployment URL (e.g., https://your-app.replit.app) and send this URL back to your Chrome extension to display in the preview window.

What to Tell Replit to Build (Copy & Paste Prompt)
If you are using Replit (or the Replit Agent) to build this orchestrator, copy and paste the following prompt directly into Replit to set up your backend:

Build a Node.js Express backend server that integrates with the Google Gemini Interactions API using the "antigravity-preview-05-2026" model to act as an agentic app builder.

The backend must support the following:
1. EXPOSE A POST ENDPOINT (/api/build):
   - Accepts a user prompt (e.g., "Build a Kanban board app with React").
   - Accepts an optional "interactionId" to resume previous coding sessions (preserving context, files, and state).

2. CALL THE GEMINI INTERACTIONS API:
   - Use the `@google/genai` SDK.
   - Call `client.interactions.create()` with:
     - agent: "antigravity-preview-05-2026"
     - environment: "remote" (to provision a Google-hosted Linux sandbox)
   - Instruct the agent to write, test, and package the frontend app.

3. HANDLE PERSISTENT AGENT SESSIONS:
   - Save the returned `interaction.id` and `interaction.environment_id` in a local database (or simple JSON file) mapped to the user's session.
   - When the user asks for edits, call the API passing the existing interaction ID so the agent edits the existing workspace rather than starting from scratch.

4. EXPORT AND ZIP THE BUILD:
   - Instruct the agent to output the final built project files as a ZIP archive.
   - Have the backend download this ZIP, extract it to a public directory on Replit, and serve it.
   - Return two things to the client:
     - A "previewUrl" (hosting the extracted files on Replit).
     - The "interactionId" (for subsequent edits).
Updating Your Chrome Extension Code
To wire this up, you need to modify how your Chrome Extension interacts with the backend.

1. Modify ai-chat.js to Call Your Replit Backend
In your extension's ai-chat.js (around line 7363 where handleSend() is defined) [2], instead of calling the raw Gemini API directly, point it to your new Replit server:

// Replace your direct streamGenerateContent call with a call to your Replit backend
async function sendToReplitBuilder(userPrompt) {
  const savedInteractionId = await chrome.storage.local.get('current_interaction_id');

  const response = await fetch('https://your-replit-project-url.replit.app/api/build', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      prompt: userPrompt,
      interactionId: savedInteractionId || null
    })
  });

  const data = await response.json();
  
  if (data.interactionId) {
    // Save the interaction ID so future prompts continue in the same sandbox
    await chrome.storage.local.set({ 'current_interaction_id': data.interactionId });
  }

  return data.previewUrl; // This is the live URL of your built site
}
2. Update the Preview Panel
Instead of loading a base64 string or raw HTML into your iframe via _lastBuiltCode [2], simply set the iframe's src attribute to the previewUrl returned by Replit:

// Inside your UI rendering logic:
const iframe = document.getElementById('previewIframe');
iframe.src = data.previewUrl; // Loads the fully compiled, multi-file application
Sources
Google Gemini API Documentation: Details on the antigravity-preview-05-2026 model and the Interactions API.
Google Cloud / Developer Blog (May/June 2026): Transitioning from Google AI Studio to Google Antigravity 2.0 and agentic workflows.
Your Extension Source Files: Structural analysis of popup.js and ai-chat.js [2].
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
Summary
To completely upgrade your Chrome Extension (SnapToAI / Aion) to match the capability of Google's Antigravity 2.0, you must shift from a static single-file generator to a persistent, agentic workspace backend.

Below is the complete, production-grade guide and code you should give to Replit (or implement yourself) to transition your system to the Gemini Interactions API using the antigravity-preview-05-2026 model. This setup provisions a secure, Google-hosted Linux sandbox, manages files dynamically, and allows the agent to self-heal code before delivering it to your extension.

Key Findings: The Architecture Shift
Feature	Your Current Extension (Static) [2]	Antigravity-Style (Agentic)
Model	Standard Gemini Models (Text Generation) [2]	antigravity-preview-05-2026
Workspace	Single-file HTML/CSS/JS in an iframe [2]	Multi-file Node/React/Vite project
Environment	Client-side Browser Iframe [2]	Google-hosted Ubuntu Linux Sandbox
Execution	Purely rendering, no execution [2]	Runs terminal commands, compilers, and tests
Persistence	Lost upon browser reload [2]	Reusable Sandbox State via Environment IDs
Step-by-Step Implementation Guide
Step 1: The Replit Backend (server.js)
Instruct Replit to create a Node.js Express server. This server acts as the orchestrator, handling incoming prompts from your extension, talking to the Gemini Interactions API, and serving the compiled app.

import express from 'express';
import { GoogleGenAI } from "@google/genai";
import cors from 'cors';
import fs from 'fs';
import path from 'path';

const app = express();
app.use(cors());
app.use(express.json());

// Initialize the Google Gen AI client with your API key
const client = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });

// In-memory session store (In production, use a database like Firestore or SQLite)
const sessions = {};

/**
 * API Endpoint to trigger an agentic build
 */
app.post('/api/build', async (req, res) => {
  const { prompt, sessionId } = req.body;

  try {
    let session = sessions[sessionId] || {
      environmentId: 'remote', // 'remote' provisions a fresh sandbox
      previousInteractionId: null
    };

    console.log(`Starting interaction for session: ${sessionId}. Env: ${session.environmentId}`);

    // Define the prompt with strict output directives
    const systemPrompt = `
      You are an expert full-stack developer inside an Ubuntu sandbox.
      Your task: ${prompt}
      
      Requirements:
      1. Build a modern, highly functional, beautifully styled web application.
      2. Use Vite, React, Tailwind CSS, and Lucide icons.
      3. Run "npm run build" inside the terminal to verify there are no compilation or syntax errors.
      4. If compile errors occur, read the error log and fix the files.
      5. Once compiled and completely verified, bundle the build output directory (usually 'dist') into a single ZIP file named "app_build.zip" in the root directory.
    `;

    // Call the Gemini Interactions API
    const interaction = await client.interactions.create({
      agent: "antigravity-preview-05-2026",
      input: systemPrompt,
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
      environment: session.environmentId, // Passes 'remote' to create a new sandbox, or the existing environment ID to resume
      previousInteractionId: session.previousInteractionId || undefined
    });

    console.log(`Interaction complete. ID: ${interaction.id}`);

    // Update our session store with the persistent environment and interaction states
    sessions[sessionId] = {
      environmentId: interaction.environment_id || session.environmentId,
      previousInteractionId: interaction.id
    };

    // The Antigravity agent will have written and zipped the application inside the sandbox.
    // We now request the agent to output the zipped binary or download it from the environment.
    // (Note: You can instruct the agent to output the base64 of 'app_build.zip' so you can write it to disk on Replit)
    
    const downloadPrompt = `
      Please read the "app_build.zip" file we just generated, convert it entirely to a base64 string, and output ONLY that base64 string. 
      Do not include any markdown formatting, backticks, or extra text. Just the raw base64 string.
    `;

    const downloadInteraction = await client.interactions.create({
      agent: "antigravity-preview-05-2026",
      input: downloadPrompt,
      environment: sessions[sessionId].environmentId,
      previousInteractionId: sessions[sessionId].previousInteractionId
    });

    const base64Data = downloadInteraction.output_text.trim().replace(/[\r\n\s]/g, '');
    const zipBuffer = Buffer.from(base64Data, 'base64');

    // Save the ZIP locally on Replit and extract it to serve the static app
    const userBuildDir = path.join(process.cwd(), 'public', 'builds', sessionId);
    fs.mkdirSync(userBuildDir, { recursive: true });

    const zipPath = path.join(userBuildDir, 'app_build.zip');
    fs.writeFileSync(zipPath, zipBuffer);

    // Extract the ZIP file using 'unzipper' or similar package
    import unzipper from 'unzipper';
    await fs.createReadStream(zipPath)
      .pipe(unzipper.Extract({ path: userBuildDir }))
      .promise();

    // Generate the URL where the preview is hosted on Replit
    const previewUrl = `https://${process.env.REPLIT_DEV_DOMAIN}/public/builds/${sessionId}/dist/index.html`;

    res.json({
      success: true,
      previewUrl,
      sessionId,
      agentLog: interaction.output_text // Send back the agent's thought/build logs to display in the extension
    });

  } catch (error) {
    console.error("Agent build failed:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});

// Serve the static build files publicly
app.use('/public', express.static(path.join(process.cwd(), 'public')));

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Replit Agentic Orchestrator running on port ${PORT}`);
});
Step 2: Update Your Chrome Extension (ai-chat.js)
To connect your extension to this powerful new backend, update the handleSend() function in your extension's ai-chat.js (around line 7363) [2].

Instead of compiling raw strings and injecting them into local storage [2], route the user’s request through your Replit server:

// Modify handleSend() inside your extension's ai-chat.js:
async function handleSend() {
  const userPrompt = getUserPromptInput(); // Get user's build instructions
  let sessionId = await chrome.storage.local.get('snaptoai_session_id');
  
  if (!sessionId) {
    sessionId = 'session_' + Math.random().toString(36).substring(2, 15);
    await chrome.storage.local.set({ 'snaptoai_session_id': sessionId });
  }

  showChatPlaceholder("Antigravity agent is spinning up a secure Linux sandbox and compiling your application...");

  try {
    const response = await fetch('https://your-replit-project-url.replit.app/api/build', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        prompt: userPrompt,
        sessionId: sessionId
      })
    });

    const data = await response.json();

    if (data.success) {
      // 1. Update the iframe preview panel with the live URL served by Replit
      const previewIframe = document.getElementById('previewIframe');
      previewIframe.src = data.previewUrl;

      // 2. Display the agent's thought/compilation logs in the chat panel
      appendAgentLogsToChat(data.agentLog);

      // 3. Persist the state
      await chrome.storage.local.set({ 'snaptoai_last_preview_url': data.previewUrl });
    } else {
      showChatError(`Build failed: ${data.error}`);
    }
  } catch (err) {
    showChatError(`Connection to Replit Orchestrator failed: ${err.message}`);
  }
}
Key Benefits of This Setup
True Code Resiliency (No More Broken Apps): Because the agent compiles the code using standard developer tools (npm run build, vite build) inside the Google-hosted Linux sandbox, it catches all React, Tailwind, and JavaScript errors before the code is ever sent to your browser. If a compilation fails, the agent reads its own terminal output, writes a patch, and recompiles until it is correct.
Persistent Sandboxes: Because we track the environment_id returned by the Interactions API, when a user says "Change the button color to blue", the agent doesn't rewrite the entire codebase from scratch. It modifies the existing workspace files in the same persistent environment, ensuring blazing-fast edit speeds and preserving database configurations.
Infinite Scaling: Because the final code is hosted on Replit, your built apps are no longer constrained by the browser's local sandbox or local storage limitations [2]. You can build multi-page projects, configure backends, and even connect databases natively.
Sources
Google Gemini API Documentation: Specifications for antigravity-preview-05-2026 and the Interactions API.
Google Antigravity Developer Guide (May 2026): Implementing managed agents in secure, isolated Linux sandboxes.
Your Extension Source Files: Structural analysis of popup.js and ai-chat.js [2].
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
Refactoring Your Multi-Stage Build Strategy (L1/L2/L3) to Agentic Workflows
In your current implementation (ai-chat.js), you split the build process into separate stages (L1_SCAFFOLD_PROMPT, L2_DESIGN_PROMPT, and L3_ACTIVATE_PROMPT) to get around the context and token limitations of traditional, stateless text generation models [2].

With the Antigravity Agent (antigravity-preview-05-2026), this manual separation is no longer necessary. The agent operates inside a 16GB Ubuntu Linux Sandbox and possesses a 1.0M token context window.

Here is how your existing codebase state variables and UI flows map to the new agentic architecture:

1. Eliminating Manual Multi-Stage Files (L1, L2, L3)
How it works now: You manually request HTML (L1), then CSS (L2), then JS (L3), and assemble them in your extension using snaptoai_build_body, snaptoai_build_style, and snaptoai_build_script [2].
The Agentic Upgrade: When the user clicks the Build button, send a single prompt to the Antigravity Agent. The agent will autonomously:
Scaffold a modern React/Vite project structure.
Write modular components (separating layout, styles, and logic cleanly).
Install dependencies (e.g., Tailwind CSS, Lucide Icons, Framer Motion).
Run a compiler (npm run build) to guarantee there are no syntax or bundler errors.
What to tell Replit to build:
Configure your Replit backend to handle a single /api/build endpoint that bypasses the manual buildStage checks [2] and executes a unified agentic loop:

// Replit Backend: Routing unified builds
app.post('/api/build', async (req, res) => {
  const { prompt, sessionId } = req.body;
  
  // The system prompt replaces BUILD_SYSTEM_PROMPT, L1, L2, and L3
  const agentPrompt = `
    You are an elite frontend engineer. Create a fully functional web application based on this request: "${prompt}".
    
    Tasks:
    1. Initialize a clean React + Tailwind CSS project.
    2. Build the application with modular components.
    3. Run a build check and fix any compilation errors.
  `;

  const interaction = await client.interactions.create({
    agent: "antigravity-preview-05-2026",
    input: agentPrompt,
    environment: "remote" // Provisions a fresh, secure Linux sandbox
  });

  // Save the environment ID to state for future edits
  saveSessionEnvironment(sessionId, interaction.environment_id);
  
  res.json({ success: true, previewUrl: await packageAndServe(interaction) });
});
2. Replacing String Patches with Native Filesystem Tools
How it works now: In ai-chat.js, you use BUILD_PATCH_PROMPT and L_UPDATE_PROMPT to perform "surgical edits" on _lastBuiltCode using regular expressions or section replacement [2]. This frequently breaks the code when the AI outputs slightly unexpected formatting [2].
The Agentic Upgrade: Because the Antigravity Agent has access to native filesystem tools, it does not need to perform fragile string replacements. When a user asks for an edit, the agent reads the specific file, edits the exact lines, and recompiles the project.
What to tell Replit to build:
When an edit or update prompt is sent from the extension, have your Replit server pass the existing environment_id and instruct the agent to modify the workspace files directly:

// Replit Backend: Handling surgical updates
app.post('/api/edit', async (req, res) => {
  const { editPrompt, sessionId } = req.body;
  const session = getSession(sessionId);

  const agentPrompt = `
    The user wants to make this modification to the existing app: "${editPrompt}".
    Use your filesystem tools to locate the relevant components, apply the changes cleanly, and run a build check to ensure no errors were introduced.
  `;

  // Resuming the session inside the same persistent remote environment
  const interaction = await client.interactions.create({
    agent: "antigravity-preview-05-2026",
    input: agentPrompt,
    environment: session.environmentId, // Keeps the exact same sandbox files intact
    previousInteractionId: session.lastInteractionId
  });

  res.json({ success: true, previewUrl: await packageAndServe(interaction) });
});
3. Upgrading Media Staging (_pendingBuildImages/Videos/Audio)
How it works now: Your extension converts images/videos to base64 strings and swaps them into the final HTML file using placeholders (SNAP_VID_N) [2]. This results in massive HTML files that slow down the browser and often exceed local storage limits [2].
The Agentic Upgrade: Send the staged media files directly to your Replit backend. Replit will write them to an /assets folder inside the build directory. The agent can then reference these local paths (e.g., <img src="/assets/hero-bg.png" />) instead of embedding huge base64 strings [2].
What to tell Replit to build:
Provide your Replit server with an /api/upload-assets endpoint:

// Replit Backend: Handling asset staging
app.post('/api/upload-assets', async (req, res) => {
  const { sessionId, assets } = req.body; // assets is an array of { filename, base64 }
  
  const userBuildDir = path.join(process.cwd(), 'public', 'builds', sessionId);
  const assetsDir = path.join(userBuildDir, 'src', 'assets');
  fs.mkdirSync(assetsDir, { recursive: true });

  for (const asset of assets) {
    const buffer = Buffer.from(asset.base64, 'base64');
    fs.writeFileSync(path.join(assetsDir, asset.filename), buffer);
  }

  res.json({ success: true, message: "Assets staged successfully in the workspace." });
});
Key Benefits of the Upgraded Architecture
No More Code Corruption: The Antigravity Agent tests the code in a real terminal before showing it to the user. If a library is missing, it runs npm install autonomously.
Infinite App Scale: Your extension is no longer limited by single-file size limits [2]. The agent can build complex multi-page applications with routers, state management, and asset directories.
Blazing-Fast Edits: By reusing the persistent environment_id, the agent only edits changed files, compiling in seconds instead of rewriting the entire application.
Sources
Google Gemini API Documentation: Specifications for antigravity-preview-05-2026 and the Interactions API.
Google Cloud Developer Blog (May 2026): Managed Agents in the Gemini API and Antigravity 2.0 integration.
Your Extension Source Files: Structural analysis of popup.js and `ai