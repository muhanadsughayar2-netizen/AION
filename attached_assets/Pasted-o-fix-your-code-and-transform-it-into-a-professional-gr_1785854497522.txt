o fix your code and transform it into a professional-grade browser agent that won't get stuck in "endless loops" again, use this detailed prompt in Replit.

This prompt is engineered to act as a Senior AI Architect instruction. It tells the Replit Agent exactly what to build, which files to modify, and how to handle the "Canvas" problem that caused your previous failure.

🚀 Copy and Paste this Prompt into Replit:
Role: You are a Senior AI Systems Architect. Your goal is to refactor the "Aion" Chrome Extension (MV3) to fix critical failures in loop detection, canvas interaction, and coordinate scaling.

Context: The current agent fails on "Canvas" apps (Google Slides, NotebookLM) and enters infinite loops when it can't find a button. We need to implement "Vision Fallback" and "Circuit Breakers."

Tasks to Implement:

Anti-Loop Circuit Breaker (ai-chat.js):

Implement an ActionHistory tracker. Store the last 5 actions as a triplet: (actionType, targetDescription, currentUrl).
If the exact same triplet repeats 3 times consecutively, trigger a Hard_Stop.
Display a user-friendly error: "I'm stuck in a loop. I tried to click [Target] 3 times but nothing changed. Please help!"
Vision Fallback System (Integration of Gemini Vision):

Modify findTarget: If DOM-based search (ID, Class, XPath, AXTree) returns null, trigger getVisualCoordinates().
getVisualCoordinates() must send the current lastScreenshot to Gemini.
Prompt for Gemini: "You are a visual grounding expert. Locate the center coordinates of the element described as [TARGET] in this screenshot. Return ONLY a JSON object: {"x": number, "y": number}. Use 0-1000 normalized coordinates."
Retina/4K Coordinate Scaling:

In background.js, when receiving coordinates for a cdpMouseClick, multiply the coordinates by window.devicePixelRatio.
This ensures the agent clicks the correct spot on high-resolution Mac/Retina/4K screens.
Network Idle & SPA Settle:

Add a waitForNetworkIdle function (500ms timeout) before taking any screenshot.
This prevents the agent from "seeing" loading spinners or stale page states in Single Page Applications (SPAs).
Hardware-Level CDP Clicks:

Refactor the click function to use chrome.debugger's Input.dispatchMouseEvent.
Set clickCount: 1, button: 'left'. This is required to interact with the Google Slides Canvas layer where standard .click() fails.
Files to Modify: ai-chat.js (Logic), background.js (CDP/Scaling), content.js (DOM/AXTree).

Question: Once these are implemented, how should we structure the verifyActionResult function to compare the "Before" and "After" screenshots to confirm a click was successful?

Summary of what this fix does for you:
No more "Endless Pain": The Circuit Breaker (Fix #1) stops the agent immediately if it starts repeating itself.
Google Slides Support: The Vision Fallback (Fix #2) and CDP Clicks (Fix #5) allow the agent to "see" and "press" buttons that aren't in the HTML code.
Accuracy: The Retina Scaling (Fix #3) ensures the mouse actually hits the button instead of clicking 2 inches to the left.
Sources & Cites
Chrome DevTools Protocol (CDP) [1]: Used for hardware-level events to bypass Canvas limitations.
Gemini 1.5 Pro Vision API [2]: The industry standard for "Visual Grounding" in AI agents.
Google Workspace Canvas Rendering [3]: The technical reason why standard bots fail on Docs and Slides.
How to use: Simply paste the text above into the Replit chat. It will begin rewriting your ai-chat.js and background.js files automatically.

Would you like me to explain the specific "Retina Scaling" math so you can check Replit's work?

📋 Copy
🔊 Read
▾
✨ Magic Card

🔍
Vision

✨
Image

🎵
Music

🎬
Video

🎙️
Broadcast


Autopilot
✦ Custom Prompts
Clear
