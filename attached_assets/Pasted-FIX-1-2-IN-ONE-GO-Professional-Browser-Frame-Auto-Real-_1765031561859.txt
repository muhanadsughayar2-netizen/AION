FIX #1 + #2 IN ONE GO: Professional Browser Frame + Auto Real URL (exactly like GoFullPage)

Do these two things together:

────────────────────────
1. Browser Frame – Stop Content Being Cut Off
- Add 40px transparent padding-top to the main captured image canvas so the frame never covers real content
- Make the frame container position: absolute, z-index: 9999, pointer-events: none
- When "macOS Style" or "Windows Style" is selected: frame floats perfectly on top
- When "None" is selected: remove frame + remove the 40px padding
- Keep our existing Border (thick/thin/square) options – they stay
- Add "URL on top" / "URL on bottom" toggle (exactly like GoFullPage)

2. Auto-Fill the Real URL – No Manual Typing
- In content.js, when capture finishes, send the real page URL to the editor:
  chrome.runtime.sendMessage({action: "editorOpen", url: window.location.href});

- In annotate.js (or popup.js), listen for that message:
  chrome.runtime.onMessage.addListener((msg) => {
    if (msg.action === "editorOpen") {
      const urlField = document.getElementById("frame-url");
      if (urlField) urlField.value = msg.url;
    }
  });

- Remove any manual URL input box → replace with read-only styled bar:
  <input id="frame-url" readonly style="background:#f5f5f5; font:12px Menlo; color:#000; border:none; width:100%; text-align:center;">

- If page has no URL (chrome://, data://, etc.) → show "Untitled Page"

- Style the URL bar exactly like a real browser (light gray, rounded, subtle shadow)

Files to touch:
- content.js → add the sendMessage
- annotate.js / popup.js → receive message + set URL
- style.css → frame container absolute + padding fix
- HTML → replace URL input with readonly version

Result after this:
→ Frame sits perfectly on top, never eats content
→ Real URL auto-appears (https://twitter.com/elonmusk… etc.)
→ Looks and feels 100% professional

One commit. Sync everything. Reload extension.

Do it now → I test → then we move to next polish (light theme, cleaner toolbar, etc.).

Go!