P1, P2, P5 Integration into performFullPageCapture (Replace your existing function)
This is the most critical change—where all the structural fixes are used.

JavaScript

// === MODIFIED performFullPageCapture (P1/P2/P5 INTEGRATION) ===
async function performFullPageCapture(tabId) {
    isFullPageCaptureRunning = true;
    let originalScrollTop = 0;
    let stickyElements = [];
    
    // P1: Find the correct scroll container
    const scrollContainer = findTheScrollContainer();
    const isWindowScroll = scrollContainer === window;
    
    // Get original scroll offset
    originalScrollTop = isWindowScroll ? window.scrollY : scrollContainer.scrollTop;

    try {
        // P5: Hide modals and pop-ups first
        hideModalsAndOverlays();
        
        // P2: Capture dynamic media
        captureCanvasAndVideo();

        // Hide sticky headers/footers
        stickyElements = hideStickyElements(); 

        // Ensure we start at the top
        if (isWindowScroll) {
            window.scrollTo(0, 0);
        } else {
            scrollContainer.scrollTop = 0;
        }

        // --- CORE LOOP SETUP (using P1 variables) ---
        
        // P1: Use the correct properties for height/view/scroll
        const fullHeight = isWindowScroll ? document.documentElement.scrollHeight : scrollContainer.scrollHeight;
        const viewportHeight = isWindowScroll ? window.innerHeight : scrollContainer.clientHeight;
        
        // ... (Rest of your capture loop logic using fullHeight, viewportHeight, and the scrollContainer to scroll) ...
        
        // --- CLEANUP ---
        
    } finally {
        // P1/P2/P5 FINAL CLEANUP
        
        // Restore scroll position
        if (isWindowScroll) {
            window.scrollTo(0, originalScrollTop);
        } else {
            scrollContainer.scrollTop = originalScrollTop;
        }
        
        // P5: Restore modals
        restoreModalsAndOverlays();
        
        // P2: Restore videos/canvases
        restoreCanvasAndVideo();
        
        // Restore sticky elements
        restoreStickyElements(stickyElements);

        removeFullPageOverlay();
        isFullPageCaptureRunning = false;
    }
    
    // ... (Your existing logic to return the result to the background script) ...
}