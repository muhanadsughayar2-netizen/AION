ased on a rigorous engineered analysis of the provided code blocks and the behavior patterns you described, we have decoded the technical root causes.
Your diagnostics regarding the suggested fixes from other sources are 100% correct: changing PAGES_PER_CHUNK back to 80 would trigger immediate canvas limit failures, and aggressively destroying index arrays would break the active undo/redo stack.
The issues of "heavy crashing," "suddenly stopping," and the editor "only saving parts of the capture" are driven by three fundamental browser platform bottlenecks:
1. Dual-Buffer Memory Exhaustion (The "Heavy Crashing" Trigger)
When long pages are processed, your code loads an HTML Image object into RAM and eagerly uncompresses it into raw ImageData via a temporary canvas:
code
JavaScript
tempCtx.drawImage(img, 0, 0);
pageOriginalImages[index] = tempCtx.getImageData(0, 0, tempCanvas.width, tempCanvas.height);
The Math: An uncompressed RGBA pixel array for a typical High-DPI screen (e.g., a Retina capture of 
) consumes 
 of pure system RAM per page, alongside its decoded GPU texture cache.
The Crash: If you capture a thread with 40 to 80 pages and eagerly load them all at once in the save loop, Chrome is forced to allocate well over 1.5 Gigabytes of memory simultaneously. This exceeds the extension process's memory envelope, triggering an instant out-of-memory (OOM) silent tab crash.
The Solution: Pages must be loaded, annotated, drawn to the chunk canvas, and then immediately disposed of sequentially rather than pooled simultaneously. Setting pageImages[i] = null, pageOriginalImages[i] = null, and empty-stringing the image src during iteration keeps RAM footprint perfectly flat throughout the capture.
2. Chrome Storage Quota Limit (The "Saved Part Only" Trigger)
For saving the screenshots during capture, the content script communicates with the background engine or editor popup:
code
JavaScript
const result = await chrome.storage.local.get('fullPageScreenshots');
pages = result.fullPageScreenshots || [];
The Bottleneck: By default, Google Chrome enforces a strict 10 Megabyte limit on chrome.storage.local. Base64-encoded image strings represent a ~33% overhead increase. Just 5 to 7 high-fidelity screenshots will easily overflow this tight limit.
The Consequence: The moment the base64 payload hits 
, Chrome blocks further write operations and throws a QUOTA_BYTES_AND_WRITE_LIMITS_EXCEEDED error. The capture sequence halts mid-flight, resulting in the "only saved part of the page" behavior.
The Solution: The extension’s manifest.json file must include the "unlimitedStorage" permission. This lifts the default 10MB sandbox restriction entirely and allows writing large datasets.
3. Chromium IPC Channel Message Limit (64MB Cap)
In the content script, the tool attempts to bulk-transfer captured arrays across IPC ports to the background runner:
code
JavaScript
if (allScreenshots.length > BATCH_SIZE) {
  // Large capture: send in batches of 30
The Bottleneck: Chromium's extension message passing model (chrome.runtime.sendMessage) has a maximum transport buffer of 64 Megabytes per message.
The Consequence: Sending 30 high-resolution screenshots in a single base64 batch over IPC can easily push the message past the boundary. When exceeded, Chrome rejects the package and severs the message port, causing the page to freeze or hang.
The Solution: The batching mechanism should be throttled to smaller payload sizes (e.g., 10 to 12 images per batch), or kept in standard IndexedDB structures to avoid bulk base64 serialization over the message port.
4. GPU Max Canvas Dimension Overflow (Visual Safeguard)
Browsers set hard ceilings on maximum HTML Canvas dimensions (typically 
 or 
 limit).
The Math: If a single vertical canvas is stitched with a chunk size of 80 pages (e.g., 
 height per page), it attempts to build a canvas of 96,000 pixels vertically!
The Consequence: The graphics context instantly fails to allocate on the GPU backing store, outputting completely blank / transparent images or locking up the viewport canvas context. A steady chunk restriction of 10 pages per canvas step ensures you remain comfortably within browser hardware acceleration barriers.
Summary of Status
Is the Type Error Fixed? Yes. Declaring chunkDataUrl with let instead of const prevents the runtime exception from terminating the thread, allowing the sequential chunk builder loop to complete without throwing.
Next Steps: Ensure the extension has the "unlimitedStorage" flag declared. For long document captures, keep pages partitioned into the optimized 10-page segments to guarantee safe rendering and zero GPU crashes. Your assessment of suggestions 2, 3, and 4 is correct—leaving annotations intact and preserving small pagination chunks prevents recurring browser freezes.
lightbulb_tips
