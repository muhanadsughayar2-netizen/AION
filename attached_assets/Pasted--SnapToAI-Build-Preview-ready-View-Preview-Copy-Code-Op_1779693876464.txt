🏗️ SnapToAI Build Preview
— ready
👁 View Preview
Copy Code
🔗 Open Standalone
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Bio-Metabolic Architect | Biological Optimization</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #F9FBF9;
      --text: #0A2E36;
      --surface: #ffffff;
      --accent: #FF7043; /* Blood Orange */
      --sage: #76A08A;
      --sage-light: #E8F0EB;
      --border: rgba(10, 46, 54, 0.08);
      --radius: 4px;
      --max-width: 1100px;
    }

    *, *::before, *::after { 
      box-sizing: border-box; 
      margin: 0; 
      padding: 0; 
    }

    html { 
      scroll-behavior: smooth; 
      -webkit-font-smoothing: antialiased;
    }

    body { 
      background-color: var(--bg);
      color: var(--text);
      font-family: 'Inter', sans-serif;
      line-height: 1.7;
      overflow-x: hidden;
    }

    h1, h2, h3 {
      font-family: 'Playfair Display', serif;
      font-weight: 400;
      line-height: 1.15;
      letter-spacing: -0.01em;
    }

    a { text-decoration: none; color: inherit; }
    ul { list-style: none; }

    .container {
      max-width: var(--max-width);
      margin: 0 auto;
      padding: 0 24px;
    }

    /* Navigation */
    nav {
      position: fixed;
      top: 0;
      width: 100%;
      background: rgba(249, 251, 249, 0.9);
      backdrop-filter: blur(12px);
      z-index: 1000;
      border-bottom: 1px solid var(--border);
    }

    .nav-inner {
      height: 90px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 1.4rem;
      font-weight: 700;
      letter-spacing: -0.5px;
    }

    .nav-links {
      display: flex;
      gap: 40px;
      font-size: 0.85rem;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .btn {
      display: inline-block;
      padding: 18px 40px;
      background: var(--accent);
      color: white;
      border-radius: var(--radius);
      font-weight: 600;
      transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
      cursor: pointer;
      border: none;
      letter-spacing: 0.05em;
      font-size: 0.9rem;
      text-align: center;
    }

    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 15px 30px rgba(255, 112, 67, 0.25);
    }

    /* Hero */
    .hero {
      padding-top: 180px;
      padding-bottom: 100px;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 80px;
      align-items: center;
    }

    .hero-content h1 {
      font-size: clamp(3.2rem, 7vw, 4.8rem);
      margin-bottom: 32px;
    }

    .hero-content p {
      font-size: 1.25rem;
      color: rgba(10, 46, 54, 0.75);
      margin-bottom: 48px;
      max-width: 520px;
    }

    .hero-image-wrap {
      position: relative;
      height: 600px;
      border-radius: 2px;
      overflow: hidden;
      box-shadow: 40px 40px 0px var(--sage-light);
    }

    .hero-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    /* Editorial Pillars Section */
    .pillars-section {
      padding: 120px 0;
      background: var(--bg);
    }

    .editorial-header {
      text-align: center;
      margin-bottom: 80px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    .editorial-header h2 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      margin-bottom: 24px;
    }

    .pillars-grid {
      display: grid;
      grid-template-columns: repeat(6, 1fr);
      grid-template-rows: repeat(2, 400px);
      gap: 20px;
    }

    .pillar-item {
      position: relative;
      overflow: hidden;
      background: #eee;
      border-radius: 2px;
    }

    /* Grid Mapping */
    .p1 { grid-column: 1 / 4; grid-row: 1; }
    .p2 { grid-column: 4 / 7; grid-row: 1; }
    .p3 { grid-column: 1 / 3; grid-row: 2; }
    .p4 { grid-column: 3 / 5; grid-row: 2; }
    .p5 { grid-column: 5 / 7; grid-row: 2; }

    .pillar-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 1.2s ease;
      filter: brightness(0.9);
    }

    .pillar-item:hover .pillar-img {
      transform: scale(1.05);
    }

    .pillar-overlay {
      position: absolute;
      bottom: 0; left: 0; width: 100%;
      padding: 32px;
      background: linear-gradient(to top, rgba(10, 46, 54, 0.9), transparent);
      color: white;
    }

    .pillar-overlay h3 { font-size: 1.8rem; margin-bottom: 8px; }
    .pillar-overlay p { font-size: 0.9rem; opacity: 0.8; font-weight: 300; }
    .pillar-tag { font-size: 0.7rem; text-transform: uppercase; letter-spacing: 0.2em; opacity: 0.6; margin-bottom: 8px; display: block; }

    /* Interactive Assessment Section */
    .bio-audit-section {
      padding: 140px 0;
      background: var(--sage-light);
    }

    .audit-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 100px;
      align-items: center;
    }

    .gauge-box {
      background: white;
      padding: 80px 40px;
      border-radius: 4px;
      text-align: center;
      box-shadow: 0 40px 80px rgba(10, 46, 54, 0.05);
    }

    .svg-gauge {
      width: 260px;
      height: 260px;
      margin: 0 auto 32px;
      position: relative;
    }

    .svg-gauge svg {
      transform: rotate(-90deg);
    }

    .gauge-bg { fill: none; stroke: #f0f0f0; stroke-width: 8; }
    .gauge-fill { 
      fill: none; 
      stroke: var(--accent); 
      stroke-width: 8; 
      stroke-dasharray: 628; 
      stroke-dashoffset: 628; 
      transition: stroke-dashoffset 0.8s cubic-bezier(0.4, 0, 0.2, 1);
      stroke-linecap: round;
    }

    .gauge-text {
      position: absolute;
      top: 50%; left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
    }

    .gauge-text .val { font-size: 4rem; font-family: 'Playfair Display', serif; font-weight: 700; display: block; line-height: 1; }
    .gauge-text .label { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.1em; opacity: 0.5; margin-top: 8px; display: block; }

    .audit-controls h2 { font-size: 3rem; margin-bottom: 24px; }
    .audit-controls p { margin-bottom: 40px; color: rgba(10, 46, 54, 0.7); }

    .food-selection {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }

    .food-btn {
      background: white;
      border: 1px solid var(--border);
      padding: 16px 20px;
      border-radius: 4px;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 0.9rem;
      transition: all 0.2s ease;
      font-weight: 500;
    }

    .food-btn:hover { border-color: var(--sage); }
    .food-btn.active { 
      background: var(--text); 
      color: white; 
      border-color: var(--text);
    }

    .food-btn .plus { 
      width: 20px; height: 20px; 
      background: var(--sage-light); 
      color: var(--sage);
      border-radius: 50%;
      display: grid; place-items: center;
      font-size: 14px;
      transition: all 0.2s ease;
    }

    .food-btn.active .plus { background: var(--accent); color: white; transform: rotate(45deg); }

    /* Pricing */
    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 32px;
      margin-top: 80px;
      padding-bottom: 120px;
    }

    .price-card {
      background: white;
      padding: 60px 40px;
      border: 1px solid var(--border);
      display: flex;
      flex-direction: column;
      transition: transform 0.3s ease;
    }

    .price-card:hover { transform: translateY(-5px); }
    .price-card.featured { border-color: var(--sage); position: relative; box-shadow: 0 20px 40px rgba(118, 160, 138, 0.1); }
    .price-card.featured::after {
      content: "POPULAR";
      position: absolute;
      top: 0; left: 50%; transform: translate(-50%, -50%);
      background: var(--sage); color: white; font-size: 0.6rem; letter-spacing: 0.2em; padding: 4px 12px;
    }

    .price-amount { font-size: 3rem; font-family: 'Playfair Display'; margin: 32px 0; }
    .price-amount span { font-size: 1rem; opacity: 0.4; font-family: 'Inter'; }

    .price-features li { font-size: 0.95rem; margin-bottom: 12px; opacity: 0.7; display: flex; gap: 10px; align-items: center; }
    .price-features svg { width: 16px; height: 16px; stroke: var(--sage); stroke-width: 2.5; fill: none; }

    footer { background: var(--text); color: white; padding: 100px 0 60px; font-size: 0.85rem; }
    .footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr; gap: 80px; }
    .disclaimer { margin-top: 60px; padding-top: 40px; border-top: 1px solid rgba(255,255,255,0.1); opacity: 0.4; line-height: 1.6; }

    /* Reveal */
    .reveal { opacity: 0.85; transform: translateY(20px); transition: all 0.8s cubic-bezier(0.165, 0.84, 0.44, 1); }
    .reveal.visible { opacity: 1; transform: translateY(0); }

    @media (max-width: 900px) {
      .hero-grid, .audit-container, .pricing-grid, .footer-grid { grid-template-columns: 1fr; }
      .pillars-grid { grid-template-columns: 1fr; grid-template-rows: auto; }
      .pillar-item { height: 350px; grid-column: auto !important; grid-row: auto !important; }
      .hero-image-wrap { height: 400px; order: -1; }
    }
  </style>
</head>
<body>

  <nav>
    <div class="container nav-inner">
      <a href="#" class="logo">BIO-ARCHITECT</a>
      <ul class="nav-links">
        <li><a href="#pillars">The Pillars</a></li>
        <li><a href="#audit">Bio-Audit</a></li>
        <li><a href="#pricing">Plans</a></li>
      </ul>
      <a href="#pricing" class="btn" style="padding: 12px 28px; font-size: 0.75rem;">START MY PLAN</a>
    </div>
  </nav>

  <header class="hero container">
    <div class="hero-grid">
      <div class="hero-content">
        <h1 class="reveal">Eat More. <br><i>Repair</i> Faster.</h1>
        <p class="reveal">Welcome to the world of bio-fortification. We don't count calories; we count the molecular activations that hardwire your body for longevity.</p>
        <div class="reveal">
          <a href="#audit" class="btn">TRY THE BIO-AUDIT</a>
        </div>
      </div>
      <div class="hero-image-wrap reveal">
        <img src="https://images.unsplash.com/photo-1590080876174-8977f686810b?w=1200&q=80" alt="Vibrant food macro" class="hero-image">
      </div>
    </div>
  </header>

  <section id="pillars" class="pillars-section container">
    <div class="editorial-header reveal">
      <span class="pillar-tag" style="opacity:1; color: var(--sage);">Our Framework</span>
      <h2>The 5 Health Defense Shields</h2>
      <p>Decades of research have uncovered five distinct biological systems that protect our health from birth. By choosing specific "bioactive" foods, we can upgrade these shields in real-time.</p>
    </div>

    <div class="pillars-grid">
      <div class="pillar-item p1 reveal">
        <img src="https://images.unsplash.com/photo-1544787210-282aa90848f4?w=800&q=80" class="pillar-img" alt="Green tea macro">
        <div class="pillar-overlay">
          <span class="pillar-tag">System 01</span>
          <h3>Angiogenesis</h3>
          <p>Managing blood vessel growth to maintain balance and starve fat cells.</p>
        </div>
      </div>
      <div class="pillar-item p2 reveal">
        <img src="https://images.unsplash.com/photo-1543208930-dc4226993ead?w=800&q=80" class="pillar-img" alt="Walnuts macro">
        <div class="pillar-overlay">
          <span class="pillar-tag">System 02</span>
          <h3>Regeneration</h3>
          <p>Activating adult stem cells to repair organs and reverse aging markers.</p>
        </div>
      </div>
      <div class="pillar-item p3 reveal">
        <img src="https://images.unsplash.com/photo-1615485290382-441e4d0c9cb5?w=800&q=80" class="pillar-img" alt="Pear macro">
        <div class="pillar-overlay">
          <span class="pillar-tag">System 03</span>
          <h3>Microbiome</h3>
          <p>Cultivating 39 trillion gut bacteria that run your immune system.</p>
        </div>
      </div>
      <div class="pillar-item p4 reveal">
        <img src="https://images.unsplash.com/photo-1606923829579-0cb981a83e2e?w=800&q=80" class="pillar-img" alt="Green leaves macro">
        <div class="pillar-overlay">
          <span class="pillar-tag">System 04</span>
          <h3>DNA Protection</h3>
          <p>Repairing genetic damage from stress, toxins, and environmental radiation.</p>
        </div>
      </div>
      <div class="pillar-item p5 reveal">
        <img src="https://images.unsplash.com/photo-1581299894007-aaa50297cf16?w=800&q=80" class="pillar-img" alt="Mushrooms macro">
        <div class="pillar-overlay">
          <span class="pillar-tag">System 05</span>
          <h3>Immunity</h3>
          <p>Coaching your natural killer cells to identify and eliminate threats early.</p>
        </div>
      </div>
    </div>
  </section>

  <section id="audit" class="bio-audit-section">
    <div class="container audit-container">
      <div class="gauge-box reveal">
        <div class="svg-gauge">
          <svg viewBox="0 0 210 210">
            <circle class="gauge-bg" cx="105" cy="105" r="100"></circle>
            <circle class="gauge-fill" id="gauge-fill" cx="105" cy="105" r="100"></circle>
          </svg>
          <div class="gauge-text">
            <span class="val" id="score-val">0</span>
            <span class="label">Bio-Defense Score</span>
          </div>
        </div>
        <p style="font-size: 0.9rem; opacity: 0.6;">Select the foods you ate today to see the power of biological activation.</p>
      </div>

      <div class="audit-controls">
        <h2 class="reveal">The Power of Choice</h2>
        <p class="reveal">See how small additions to your daily routine can dramatically shift your metabolic defense. This real-time auditor shows the "Score" we track weekly with our clients.</p>
        
        <div class="food-selection reveal">
          <button class="food-btn" data-value="18">
            <span class="plus">+</span> Broccoli (DNA)
          </button>
          <button class="food-btn" data-value="15">
            <span class="plus">+</span> Green Tea (Angio)
          </button>
          <button class="food-btn" data-value="20">
            <span class="plus">+</span> Walnuts (Regen)
          </button>
          <button class="food-btn" data-value="12">
            <span class="plus">+</span> Berries (Angio)
          </button>
          <button class="food-btn" data-value="17">
            <span class="plus">+</span> Sauerkraut (Gut)
          </button>
          <button class="food-btn" data-value="18">
            <span class="plus">+</span> Mushrooms (Immune)
          </button>
        </div>
      </div>
    </div>
  </section>

  <section id="pricing" class="container">
    <div class="editorial-header reveal">
      <h2>Invest in Longevity</h2>
      <p>Personalized bio-architecture for every stage of your journey.</p>
    </div>

    <div class="pricing-grid">
      <div class="price-card reveal">
        <h3>Roadmap</h3>
        <p style="opacity:0.6; font-size:0.9rem;">One-time Bio-Audit</p>
        <div class="price-amount">$50</div>
        <ul class="price-features">
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> Full Bio-Assessment</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> Personalized Food Library</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> 5-Shield Strategy</li>
        </ul>
        <button class="btn" style="background:transparent; border:1px solid var(--text); color: var(--text); margin-top: auto;">GET ROADMAP</button>
      </div>

      <div class="price-card featured reveal">
        <h3>Bio-Fortify</h3>
        <p style="opacity:0.6; font-size:0.9rem;">Monthly Transformation</p>
        <div class="price-amount">$100<span>/mo</span></div>
        <ul class="price-features">
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> Weekly "Fresh Sheets"</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> Personalized Adjustments</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> Priority Email Support</li>
        </ul>
        <button class="btn" style="margin-top: auto;">START TRANSFORMATION</button>
      </div>

      <div class="price-card reveal">
        <h3>VIP Defense</h3>
        <p style="opacity:0.6; font-size:0.9rem;">Executive Coaching</p>
        <div class="price-amount">$120<span>/mo</span></div>
        <ul class="price-features">
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> Everything in Fortify</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> 24/7 WhatsApp Concierge</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17L4 12"/></svg> Bi-Weekly Video Calls</li>
        </ul>
        <button class="btn" style="background:transparent; border:1px solid var(--text); color: var(--text); margin-top: auto;">APPLY FOR VIP</button>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <div class="footer-grid">
        <div>
          <span class="logo" style="margin-bottom: 24px; display: block;">BIO-ARCHITECT</span>
          <p style="opacity: 0.5; max-width: 340px;">Harnessing the science of bioactives to rebuild human resilience. Not a diet. A biological upgrade.</p>
        </div>
        <div>
          <h4 style="font-size: 0.7rem; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 24px; color: var(--sage);">Science</h4>
          <ul style="opacity: 0.5; display: flex; flex-direction: column; gap: 12px;">
            <li><a href="#">The 5 Shields</a></li>
            <li><a href="#">Methodology</a></li>
            <li><a href="#">Clinical Research</a></li>
          </ul>
        </div>
        <div>
          <h4 style="font-size: 0.7rem; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 24px; color: var(--sage);">Inquiry</h4>
          <ul style="opacity: 0.5; display: flex; flex-direction: column; gap: 12px;">
            <li><a href="#">Contact Coach</a></li>
            <li><a href="#">Privacy Policy</a></li>
            <li><a href="#">Member Portal</a></li>
          </ul>
        </div>
      </div>
      <div class="disclaimer">
        MEDICAL DISCLAIMER: The information provided on this site is for educational purposes only and is not intended as medical advice. I am a Health Coach, not a physician. Always consult with a healthcare professional before changing your diet or health routine, particularly if you have a pre-existing medical condition.
        <br><br>
        &copy; 2024 The Bio-Metabolic Architect.
      </div>
    </div>
  </footer>

  <script>
    // Reveal Animations
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

    // Interactive Bio-Audit Score Logic
    let currentScore = 0;
    const scoreVal = document.getElementById('score-val');
    const gaugeFill = document.getElementById('gauge-fill');
    const circumference = 2 * Math.PI * 100;

    document.querySelectorAll('.food-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const val = parseInt(btn.getAttribute('data-value'));
        
        if (btn.classList.contains('active')) {
          btn.classList.remove('active');
          currentScore -= val;
        } else {
          btn.classList.add('active');
          currentScore += val;
        }

        updateScore(currentScore);
      });
    });

    function updateScore(score) {
      // Ensure score stays in 0-100 bounds for the visual
      const visualScore = Math.min(Math.max(score, 0), 100);
      
      // Animate Number
      const start = parseInt(scoreVal.textContent);
      const duration = 600;
      const startTime = performance.now();

      function animate(now) {
        const elapsed = now - startTime;
        const progress = Math.min(elapsed / duration, 1);
        const currentNum = Math.floor(start + (score - start) * progress);
        scoreVal.textContent = currentNum;

        if (progress < 1) requestAnimationFrame(animate);
      }
      requestAnimationFrame(animate);

      // Update Gauge SVG
      const offset = circumference - (visualScore / 100) * circumference;
      gaugeFill.style.strokeDashoffset = offset;
    }

    // Parallax hero
    window.addEventListener('scroll', () => {
      const scroll = window.pageYOffset;
      const img = document.querySelector('.hero-image');
      if (img) img.style.transform = `translateY(${scroll * 0.1}px)`;
    });
  </script>
</body>
</html>