ou are absolutely right to hate it. That built-in Chrome voice sounds like a 2005 GPS navigation system. It completely kills the high-end, "Million-Dollar" vibe we are going for.

Replit used chrome.tts because it takes exactly one line of code to build, which is great for a quick test, but it is not what you are going to ship to your users.

We are going to rip that robot voice out right now and plug in the Gemini 3.1 Flash Live API so Snap actually sounds human.

The Game Plan
We are going to tell Replit to keep the beautiful UI (the floating face, the glowing buttons, the microphone button) but completely swap out the "mouth."

Here is the exact, forceful prompt you need to copy and paste into Replit to make it switch to the premium audio:

Copy and Paste this to Replit:

"The UI and the microphone capture in snap-coach.js and snap-coach.css are perfect. Great job keeping it modular.

However, I do NOT want to use chrome.tts for the voice. It sounds too robotic. We are building a premium app, and we must use the Gemini Multimodal Live API (WebSockets) to stream the expressive audio response back to the user.

Here is your new directive:

Remove all instances of chrome.tts.

Upgrade the API call to connect to the Gemini Live API (gemini-2.0-flash-exp or equivalent multimodal endpoint that supports native audio out).

The system prompt must instruct the AI to use an expressive, conversational voice.

We need to handle the incoming audio/pcm stream from Gemini and play it through the browser's AudioContext.

Please update snap-coach.js to handle the WebSocket connection and audio playback. Let me know if you need to create an offscreen.html document to keep the audio stream alive in Manifest V3."

What this does:
This tells the Replit AI to stop taking the easy route. It forces it to write the code that captures Google's raw, human-sounding audio stream and plays it directly through your extension.

Once you paste this into Replit, it should generate a new version of snap-coach.js. Let me know if it gets confused about the WebSockets or the AudioContext, and I will write the exact Javascript function for you to drop in.