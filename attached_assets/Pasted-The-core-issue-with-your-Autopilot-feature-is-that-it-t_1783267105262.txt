The core issue with your "Autopilot" feature is that it treats Google Sheets and Google Docs like standard HTML websites, whereas Google has re-engineered them to be Canvas-rendered applications.

Standard browser automation relies on the Document Object Model (DOM)—the tree of HTML elements like <input> or <div>. However, Google now renders the actual content of these apps as a flat image on a <canvas> element to improve performance [1.1.4, 1.1.6].

Summary of Why Your Code Fails
The "Invisible" Grid: In Google Sheets, there are no "cell" elements in the HTML. The grid is just pixels drawn on a canvas. Your CSS selectors (like .grid-container or #waffle) only find the container, not the actual input target [1.1.6, 1.3.2].
Hidden Input Targets: Both apps use a "hidden layer" to capture typing.
Docs: Uses a hidden iframe (class: docs-texteventtarget-iframe) that must be focused to receive keystrokes [1.2.1, 1.2.3].
Sheets: Only spawns a real HTML <textarea> after you double-click a cell. If your code types before this element exists, the text goes nowhere [1.1.1, 1.1.6].
Selector Instability: The class names you are using (e.g., .grid-scrollable-wrapper-3) are internal Google implementation details. Google frequently changes these names, which is why your selectors "break" even if the page looks the same [1.3.2, 1.5.1].
Key Findings & Research-Based Solutions
1. The "Google-Approved" Path: REST APIs
Google's official developer notes explicitly state that the DOM structure of Workspace apps is not a stable API and should not be used for automation [1.5.1, 1.5.8].

The Fix: Instead of simulating clicks, your extension should use the Google Sheets API (spreadsheets.values.update) and Google Docs API (documents.batchUpdate) [1.3.3].
Benefit: This is 100% reliable. It doesn't matter if a banner is in the way or if Google changes its CSS classes; the API writes directly to the data model [1.4.1].
2. The "Improved Hack": Targeting the Hidden Layer
If you must stay with browser automation, you need to target the "input target" rather than the canvas.

For Google Docs: You must send your Input.dispatchKeyEvent directly to the docs-texteventtarget-iframe. Without focusing this specific iframe, the keystrokes are ignored by the document body [1.2.3, 1.2.6].
For Google Sheets: You must simulate a double-click at the coordinates to force Sheets to spawn the hidden textarea editor, then wait for that element to appear in the DOM before typing [1.1.1].
3. Force-HTML Mode (Experimental & Fragile)
There is a hidden flag Google provides for accessibility tools to force Docs back into HTML mode:

Code: window._docs_force_html_by_ext = "your-extension-id"; [1.1.2, 2.4.1].
Risk: This is undocumented and often requires your extension to be "whitelisted" by Google. It is known to be inconsistent and may be fully deprecated soon [2.4.2, 2.4.7].
4. Visual Verification (The "Agent" Way)
Since your agent uses Gemini, it should not report "success" based on the code execution alone.

The Fix: After typing, the agent must take a second screenshot and compare it to the first. If the cell is still empty, the agent should realize it "missed" and try adjusting its click coordinates [1.1.6, 2.3.1].
Recommended Action Plan
Feature	Problem	Recommended Fix
Reliability	Selectors break constantly.	Switch to REST APIs. Use OAuth2 to write to A1 directly [1.3.3].
Accuracy	Clicks land in the title/toolbar.	Accessibility Tree. Use the browser's accessibility tree (CDP Accessibility.getFullAXTree) to find the "Grid" role instead of CSS [2.3.1, 2.3.3].
Verification	AI reports "Success" falsely.	Visual Loop. Require the AI to "see" the typed text in a post-action screenshot before finishing the task.
Sources
[1.1.4] Google Docs Switches to Canvas Rendering
[1.2.1] Google Doc's event listener iframe element
[1.3.2] DevTools Protocol blocked on Google properties
[1.3.3] Google Sheets API: Basic Writing
[2.4.1] How to force Google Docs to render HTML
[2.3.1] The Accessibility Tree for AI Agents
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
To solve the issue of your AI "typing outside the box" in Google Sheets and Docs, you must move away from standard CSS selectors and raw coordinate guessing. Because these apps use Canvas rendering, the "cells" and "pages" you see do not exist in the HTML DOM.

Summary of the Technical Failure
Google Sheets: The grid is a flat image drawn on a <canvas>. Your code finds the .grid-container, but it has no idea where "Cell A1" is within that container.
Google Docs: The text you see is also a canvas-like layer. There is a hidden "typing target" (an iframe), but it only accepts input if it is precisely focused [1.2.1, 1.2.3].
The "Success" Lie: Your background.js reports success because it successfully sent keystrokes to the browser window, but it has no way of knowing if the application accepted them [4b].
Key Findings & Implementation Plan
1. The "Accessibility Tree" (The AI Agent Standard)
Instead of guessing CSS selectors, use the Accessibility Tree. This is a semantic map Google provides for screen readers that does contain the coordinates of every cell and text block, even on a canvas [1.1.3, 1.1.5].

The Fix: Use the Chrome DevTools Protocol (CDP) command Accessibility.getFullAXTree.
Action: In your background.js, call this command to find the node with role: "gridcell" or role: "textbox". This will give you the exact x/y coordinates of the active cell, regardless of banners or toolbars [1.1.6, 1.1.8].
2. The "Double-Click" Requirement (Sheets)
Google Sheets only creates a real HTML <textarea> after a cell is activated for editing.

The Fix: Your locateForType must perform a double-click (not a single click) at the target coordinates.
Wait for Element: After double-clicking, your content.js should wait for a textarea with the class grid-editable-mode to appear. Only then should you start the Input.dispatchKeyEvent loop [1.1.1].
3. Targeting the "Text Event" Iframe (Docs)
Google Docs ignores keystrokes unless they are directed at a hidden iframe with the class .docs-texteventtarget-iframe [1.2.1].

The Fix:
Find the iframe: document.querySelector('.docs-texteventtarget-iframe').
Focus it: iframe.contentWindow.focus().
Send your CDP Input.dispatchKeyEvent to that specific frame's execution context.
4. The "Golden Path": Google Workspace APIs
Google's own developer notes state that DOM-based automation for Sheets/Docs is "unsupported and fragile" [1.5.1].

The Recommendation: Switch to the REST APIs.
Sheets: Use spreadsheets.values.update to write to Sheet1!A1 [1.5.6].
Docs: Use documents.batchUpdate to insert text at an index [1.2.8].
Why: This is 100% reliable. It works even if the tab is in the background, if there are pop-up banners, or if Google changes its CSS classes tomorrow.
Specific Code Fixes
Update your background.js to verify input visually: Instead of just returning {success: true}, implement a Visual Check Loop:

// 1. Take a screenshot BEFORE typing
const before = await captureScreenshot();
// 2. Execute typing
await sendTypingCommands();
// 3. Take a screenshot AFTER typing
const after = await captureScreenshot();
// 4. Send both to Gemini
const isDifferent = await aiAgent.compare(before, after);
if (!isDifferent) {
    return { success: false, error: "Text did not appear in the UI. Retrying..." };
}
Update your content.js for Sheets:

// Force the app to create a real input element
async function activateCell(x, y) {
  await sendCDP('Input.dispatchMouseEvent', {type: 'mousePressed', clickCount: 2, x, y, ...});
  await sendCDP('Input.dispatchMouseEvent', {type: 'mouseReleased', clickCount: 2, x, y, ...});
  
  // Wait up to 1s for the hidden textarea to spawn
  for(let i=0; i<10; i++) {
    if(document.querySelector('textarea.grid-editable-mode')) return true;
    await new Promise(r => setTimeout(r, 100));
  }
}
Sources
[1.1.3] Accessibility Tree Explorer for AI Agents
[1.2.1] Google Docs Text Event Target Iframe Documentation
[1.5.6] Google Sheets API: Reading and Writing Values
[1.1.6] Using CDP Accessibility.getFullAXTree for Automation
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
To resolve the issue where your AI Autopilot types outside of Google Sheets and Docs, you must move beyond standard CSS selectors. Because these applications render their workspace on a <canvas> element, the "cells" or "pages" do not exist as HTML elements for your code to find [1.1.1, 1.3.2].

Summary of the Problem
Google Sheets: The grid is a flat image. Your selectors find the container, but the AI clicks "near the top-left" by default, often hitting the title or toolbar [4a].
Google Docs: The document body is a virtual layer. Keystrokes are only accepted if directed at a hidden, off-screen iframe that must be manually focused [1.2.1, 1.3.1].
Gmail (The Exception): Unlike Sheets/Docs, Gmail uses a standard HTML contenteditable div. Your current code works better there because the elements are "real," though it still requires precise focus on the role="textbox" element [7, 1.4.2].
Key Findings & Solutions
1. The "Semantic" Fix: Use the Accessibility Tree
Instead of guessing CSS classes that Google changes frequently, use the Accessibility Tree. This is a hidden map Google provides for screen readers that does contain the coordinates for every cell and text block, even on a canvas [1.1.1, 1.1.3].

How to do it: Use the Chrome DevTools Protocol (CDP) command Accessibility.getFullAXTree.
The Benefit: This allows your agent to find a node with role: "gridcell" and get its exact x/y screen coordinates, bypassing the "invisible grid" problem entirely [1.1.6, 1.1.8].
2. The Interaction Fix: Double-Click & Iframe Focus
For Sheets: You cannot just click once. You must simulate a double-click at the coordinates to force Sheets to spawn a hidden HTML <textarea>. Your code should wait for an element with the class .grid-editable-mode to appear before it starts typing [1.1.1].
For Docs: Your background.js must send keystrokes specifically to the .docs-texteventtarget-iframe. If you send them to the main window, Docs ignores them [1.3.1].
3. The "Golden Path": Use Google Workspace APIs
The most reliable way to solve this is to stop "simulating" a human and start "integrating" with the app.

Implementation:
Extract the ID: Parse the Spreadsheet/Doc ID from the current URL (the long string between /d/ and /edit) [1.2.3, 1.2.7].
OAuth2: Use the chrome.identity API to get an auth token from the user.
Direct Write: Use the Google Sheets API (spreadsheets.values.update) to write directly to cell "A1" or the Google Docs API (documents.batchUpdate) to insert text at the current cursor index [1.3.2, 1.5.6].
Why this works: It works even if the tab is in the background, if a "Google Cloud Credit" banner is covering the screen, or if Google changes their internal class names [1.3.2, 1.4.1].
4. Close the Feedback Loop (Visual Verification)
Your agent currently reports "success" as soon as it sends a key event. To make it "well-functioning," it must verify visually.

The Loop: Take a screenshot → Type → Take a second screenshot → Have Gemini compare them. If the text didn't appear, the agent should adjust its coordinates and try again rather than assuming it worked [4b, 6].
Development Roadmap
Immediate Hack: Update locateForType to perform a double-click in Sheets and focus the hidden iframe in Docs [1.1.1, 1.3.1].
Mid-term Stability: Replace CSS selectors with a CDP call to Accessibility.getFullAXTree to find elements by their semantic "Role" [1.1.3, 1.1.8].
Long-term Reliability: Implement the REST API path for Sheets and Docs to ensure 100% accuracy regardless of UI changes [1.3.2, 1.5.6].
Sources
[1.1.1] Accessibility Tree vs DOM for AI Agents
[1.1.3] A11y-tree snapshots for AI Browsers
[1.2.3] Regex to extract Google Sheet ID from URL
[1.3.1] Simulating keypresses on Google Docs Iframe
[1.3.2] Automating Google Docs via official API
📋 Copy
🔊 Read
▾
✨ Magic Card
Please continue your response from where you left off.
To ensure your AI Autopilot functions reliably, you must align your code with Google’s official architectural changes. Based on Google’s developer notes and the transition to "Smart Canvas" rendering, here is the technical path to solving the "typing outside the box" issue.

1. Google’s Official "Notes" on Automation
In May 2021, Google officially announced that Google Docs would migrate to Canvas-based rendering [1.1.1].

The Warning: Google explicitly stated that this change would impact Chrome extensions that rely on the HTML DOM structure [1.2.1].
The Recommendation: Google’s official guidance for developers is to move away from DOM-scraping and instead use the Google Workspace Add-ons framework or the REST APIs (Google Sheets/Docs API) [1.2.1]. This is because the visual "grid" and "page" are now just pixels painted on a <canvas> element, making CSS selectors like .grid-container fundamentally unreliable [1.2.3, 1.2.4].
2. The "Hidden" Fix: Screen Reader Mode
There is a specific "backdoor" Google provides for accessibility tools that your extension can leverage.

The Mechanism: When a user enables "Screen Reader Support" (Tools > Accessibility settings), Google Docs/Sheets is forced to emit a "Hidden DOM" or "Shadow DOM" that contains the actual text and structure for assistive technology [1.2.6, 1.5.9].
How to solve it: Your extension can programmatically check if this mode is on or even "force" a similar state by targeting the Accessibility Tree (AOM) via the Chrome DevTools Protocol (CDP) command Accessibility.getFullAXTree [1.5.6]. This tree provides the exact coordinates of "cells" and "text blocks" that do not exist in the standard HTML tree [1.5.5].
3. Solving the Image Insertion (User Ask #7)
Your code currently cannot "paste" images because the Canvas layer doesn't have a standard "drop zone" for files.

For Google Sheets: The REST API does not support direct image blob uploads into cells [1.3.1, 1.3.4].
The Fix: Your AI should instead type the =IMAGE("URL") function into the cell. This is the only stable way to "insert" an image via text-based automation [1.3.3].
For Google Docs: Use the insertInlineImageRequest via the Docs API. This requires the image to be hosted at a publicly accessible URL [1.3.8, 1.4.1].
4. Why Gmail Works Differently
As you noted, Gmail works better because it still uses Standard HTML (contenteditable) for its compose box [7].

The Difference: In Gmail, the text you type is wrapped in real <div> and <span> tags.
The Optimization: To make Gmail 100% reliable, ensure your locateForType looks for the element with role="textbox" and contenteditable="true". Unlike Sheets, Gmail does not require a double-click to "spawn" the input area [1.4.2].
Final Optimized Implementation Strategy
Feature	The "Guessing" Way (Current)	The "Reliable" Way (Fix)
Finding Cells	CSS Selectors (#waffle)	CDP Accessibility Tree (role="gridcell") [1.5.5]
Typing	Simulating raw keys	Double-click (to spawn textarea) + Input.insertText [1.1.1]
Images	Simulating "Paste"	=IMAGE() function (Sheets) or API Request (Docs) [1.3.1]
Verification	Reporting success: true	Screenshot Comparison (Visual AI check) [6]
Sources
[1.1.1] Google Docs Canvas Migration Announcement
[1.2.1] Impact on Chrome Extensions - Google Workspace Developer Notes
[1.3.1] Sheets API Image Insertion Limitations
[1.5.5] AI Agents and the Accessibility Tree
[1.5.6] Chrome DevTools Protocol: Accessibility Tree