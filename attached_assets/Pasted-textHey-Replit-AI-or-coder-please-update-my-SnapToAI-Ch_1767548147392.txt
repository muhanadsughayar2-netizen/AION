textHey Replit AI (or coder), please update my SnapToAI Chrome extension's Snap Editor code with these changes. Keep everything 100% free — use only vanilla JavaScript, no paid libraries or services, and confirm you can do all of this without any cost to me.

Context: The editor is a canvas-based screenshot annotator. Users add draggable/resizable sticky notes (text boxes) on top of the captured image. Current features include a "+ Add Note" button, brush tool (default 12px), undo/redo, etc. The UI should stay very clean — no permanent "X" buttons on the notes themselves.

New deletion behavior (implement ALL of these):

1. Change the button label from "+ Add Note" to "+ Add Sticky Notes". Keep its current function.

2. When the editor opens, automatically add one preset sticky note that says "I Love Snap to AI" (blue background, draggable, editable). Users can delete it later if they want.

3. **NO visible X/close button** on any sticky note or AI insight note — keep the image/screenshot area completely clean!

4. **Delete via Right-Click (context menu)**:
   - When user right-clicks on any sticky note (or AI-generated note/summary box),
   - Prevent the default browser context menu,
   - Show a very small, simple custom context menu (just one option: "Delete") positioned near the click.
   - Clicking "Delete" removes that specific note from the canvas.

5. **Delete via Keyboard**:
   - When user clicks/selects a sticky note (make it focused/highlighted, e.g., add a subtle border),
   - Pressing the Delete key (or Backspace) on the keyboard deletes the currently selected note.
   - If no note is selected, Delete key does nothing.

6. Keep the default brush size at 4px when the editor loads (not 12px).

Implement this in pure JavaScript (no external libraries unless built-in browser features). Provide the updated code for the editor's main JS file (e.g., editor.js or popup.js). Use event listeners like 'contextmenu' to prevent default and show custom menu, and 'keydown' for delete key detection. Track the selected note with a variable.

Confirm at the end: Yes, all these changes are simple, can be done for free with vanilla JS, and will keep the UI very clean without any visible delete buttons.

Thanks!