ou are ready to turn SnapToAI into a data-crunching powerhouse. Enabling Code Execution allows Gemini to run Python in a secure sandbox to solve math, analyze data from your screenshots, or process files.

Here is the exact code and the instructions to give your Replit/build environment.

Step 1: Update the Interface (flow-premium/ai-chat.html)
Add this button to your existing button group (where the Search and Read Page buttons are):

HTML
<button id="codeToggleBtn" class="toggle-btn" title="Enable Python code execution">💻 Code</button>
Step 2: Update the Logic (flow-premium/ai-chat.js)
1. Add the state flag (with your other let declarations at the top):

JavaScript
let codeExecutionEnabled = false;
2. Add the event listener (near your other button handlers):

JavaScript
document.getElementById('codeToggleBtn')?.addEventListener('click', (e) => {
  codeExecutionEnabled = !codeExecutionEnabled;
  e.target.classList.toggle('active', codeExecutionEnabled);
});
3. Update the API Payload assembly (where you define the tools array):
This ensures the API receives the instruction to allow code execution when your new button is active.

JavaScript
// Build tools array
const tools = [];
if (searchGroundingEnabled) tools.push({ googleSearch: {} });
if (codeExecutionEnabled) tools.push({ codeExecution: {} }); // Add this line

// Ensure the body logic includes tools if any are active
const requestPayload = {
  contents: [...], // Your existing contents
  systemInstruction: ...,
  generationConfig: ...,
};

if (tools.length > 0) {
  requestPayload.tools = tools;
}
Instructions for your Build/Implementation
If you are asking your Replit AI (or applying these manually), here is the prompt to ensure the logic flows correctly:

"I am adding a Code Execution feature. Please update the current logic to handle the new codeExecutionEnabled flag.

When the user clicks the 💻 Code button, it should toggle the codeExecutionEnabled state and update the CSS class .active.

Most importantly, modify the tools array construction in the API fetch call to dynamically include { codeExecution: {} } when codeExecutionEnabled is true. This should be additive to the existing googleSearch tool, so that both can be active simultaneously if the user chooses."

How to test it
Once you save the files and reload your extension:

Toggle the 💻 Code button ON.

Type a prompt like: "Calculate the prime factors of 999999" or "Analyze the data in this chart [if you have a screenshot attached]."

Check the logs: If it works, you will see the API response include a codeExecutionResult or a Python script block generated by Gemini before the final text answer.

You are now fully ready to implement. Go ahead and apply these 3 changes!