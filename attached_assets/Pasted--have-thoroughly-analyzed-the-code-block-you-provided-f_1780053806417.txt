 have thoroughly analyzed the code block you provided for your full-page stitching and annotation tool. There are three clear reasons explaining why your Replit project started crashing, failing to save your screenshots, and now only saving parts of the page.
Here is the exact diagnostic breakdown of the issues and how you can resolve them in your code:
1. The Fatal TypeError Crash (Why it only "saves part")
In your saveFullPageWithAnnotations function, you declare the variable chunkDataUrl using the const keyword:
code
JavaScript
// PNG format for maximum text clarity - never use JPEG for screenshots with text
const chunkDataUrl = chunkCanvas.toDataURL('image/png');
At the end of the chunking loop, you try to manually clear the memory of this variable to prevent memory leaks:
code
JavaScript
// Free chunk data from local scope immediately
chunkDataUrl = null;
The Issue: Attempting to assign a new value to a const variable throws a fatal runtime TypeError: Assignment to constant variable.
The Symptom: This exception immediately halts execution of the chunking pipeline. Because your chrome.runtime.sendMessage call happens before this crash, Chunk 1 gets saved successfully (saving part of the page), but the script crashes before it can process Chunk 2 or Chunk 3. This explains why it suddenly stops saving the remainder of the page.
The Solution: Change const chunkDataUrl to let chunkDataUrl so reassignment is permitted, or simply remove the chunkDataUrl = null; line altogether because let/const variables in loops are garbage-collected once they exit the loop block scope.
2. Chrome Storage Quota Exhaustion (Why "it stopped/failed to save")
Your code relies heavily on saving screenshots to chrome storage:
code
JavaScript
const result = await chrome.storage.local.get('fullPageScreenshots');
pages = result.fullPageScreenshots || [];
The Issue: By default, Google Chrome places a strict 10MB quota limit on chrome.storage.local. If you capture a large webpage with dozens of high-resolution base64 screenshots, the total size of fullPageScreenshots will easily exceed 10MB.
The Symptom: Once the 10MB limit is reached, Chrome blocks any further writes to chrome.storage.local. This causes captures to suddenly fail, crash, or save empty images.
The Solution:
Add the "unlimitedStorage" permission to your extension's manifest.json file.
Use chrome.storage.session instead of chrome.storage.local to store raw, temporary captured page streams. Session storage is stored in memory and handles large base64 buffers much better.
3. Dynamic Canvas Memory Accumulation (Why it "became heavy crashing")
In your stitching loop, you create multiple heavy canvas objects dynamically inside high-frequency iterations:
code
JavaScript
for (let i = startPage; i < endPage; i++) {
  // ...
  const pageCanvas = document.createElement('canvas'); // Created every iteration
  // ...
  pageCanvas.width = 0; // Cleared, but references are retained in closure scope
  pageCanvas.height = 0;
}
The Issue: While setting .width = 0 collapses the pixel buffer, the actual canvas DOM elements and contexts remain referenced inside the scope of the active execution frame. If your pages contain dozens of large elements, browser tabs will run out of memory (DOM OOM) and crash.
The Solution: Ensure you explicitly nullify DOM references at the end of every loop turn to yield memory back to Chromium's garbage collector:
code
JavaScript
pageCanvas.width = 0;
pageCanvas.height = 0;
// Dereference
pageCanvas = null;
pageCtx = null;
How to apply the patch to your Replit code:
Locate your saveFullPageWithAnnotations function inside your file and update the chunk-handling loop to declare chunkDataUrl with let instead of const, or remove the line chunkDataUrl = null;.
This will prevent the fatal Type Error, stop the loop from crashing mid-execution, and successfully restore your full-page multi-chunk stitching pipeline!
