o help your developer on Replit implement this change correctly, you can provide the following technical specification. This code ensures the user experience stays identical while the "brain" of the extension gets smarter.🛠 Replit Implementation Guide: "Smart AI Payload"1. The Logic FlowTell Replit to implement this logic inside the AI Button Click Handler:Check: Do we have extracted text from the current tab?Condition: If text exists and is $> 1,000$ characters: Send Text + Images.Fallback: If no text or short text: Send Images only (current behavior).2. Content Script (content.js)Add this function to extract clean text from the webpage without capturing ads or menus.JavaScript// Function to grab the "Meat" of the page
function getCleanPageText() {
  // Priority selectors for main content
  const mainSelectors = ['article', 'main', '.post-content', '.article-body', '#content'];
  for (const selector of mainSelectors) {
    const element = document.querySelector(selector);
    if (element && element.innerText.length > 500) {
      return element.innerText;
    }
  }
  // Fallback to body text
  return document.body.innerText;
}

// Listen for the background script asking for text
chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
  if (request.action === "get_page_text") {
    sendResponse({ text: getCleanPageText() });
  }
});
3. Background Script (background.js)Modify the listener that handles the "Send to AI" button.JavaScriptasync function handleAISubmission(tabId, images) {
  // 1. Try to get text from the page first
  const response = await chrome.tabs.sendMessage(tabId, { action: "get_page_text" });
  const pageText = response?.text;

  if (pageText && pageText.length > 800) {
    // 2. SMART PAYLOAD: Send Text + Images as reference
    console.log("Sending text-rich payload to Gemini");
    sendToGemini({
      mode: "hybrid",
      text: pageText,
      imageCount: images.length,
      imageData: images 
    });
  } else {
    // 3. FALLBACK: Original image-only logic
    console.log("Sending image-only payload");
    sendToGemini({
      mode: "images",
      imageData: images
    });
  }
}
4. The Prompt Engineering (The "Secret Sauce")When sending to Gemini, instruct Replit to format the prompt so Gemini knows which one to prioritize.Example System Prompt:"I am providing you with the raw text of a webpage for accuracy, and the screenshots of that page for visual context (charts, layout, images). Please use the text for your primary analysis and the images to confirm visual details."5. Summary Instructions for the Replit DevCopy and paste this message to them:Project Goal: Improve AI accuracy without changing the user UI.Keep the UI/Queue exactly as it is. Users still capture and see images.Update the 'AI' button trigger: Before uploading the images to Gemini, use chrome.tabs.sendMessage to grab the innerText of the page.Prioritize Text: If text is successfully retrieved, send the text string to Gemini's prompt.Use Images as Backup: Still attach the images, but tell Gemini in the prompt: "Use the provided text for accuracy."Fallback: If the page has no text (like an Instagram image or a PDF), use the existing screenshot-only logic.Why this solves your problem:No more blurry text: Gemini reads the computer-code text, which is 100% accurate.Fast: Sending text is faster than uploading 5 high-res screenshots.Context: If there is a chart in the screenshot, Gemini can still "see" it, but it won't struggle to read the small caption text because it has the raw text.Would you like me to write a specific Gemini System Prompt that Replit can use to handle both the text and images simultaneously?